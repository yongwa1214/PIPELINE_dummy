<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.pipeline_dummy.application.cart.WishListMapper">
    <insert id="saveWishList">
        INSERT IGNORE INTO wishlist
        SET game_id = #{gameId}
            , gm_profile_id = #{gmProfileId}
            , created_at = #{createdAt}
    </insert>
    <insert id="saveCart">
        INSERT IGNORE INTO cart
        SET game_id = #{gameId}
        , gm_profile_id = #{gmProfileId}
        ,purchase_type = #{purchaseType}
        , created_at = #{createdAt}
    </insert>
</mapper>