<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.pipeline_dummy.application.purchase.giftAndRefund.GiftMapper">
    <insert id="saveGift">
        INSERT INTO gift
        (item_id, sender_id, receiver_id, message, sent_at, received_at, gift_status)
        VALUES
        <foreach collection="res" item="item" separator=",">
            (
            #{item.itemId},
            #{item.senderId},
            #{item.receiverId},
            #{item.message},
            #{item.sentAt},
            #{item.receivedAt},
            #{item.giftStatus}

            )
        </foreach>
    </insert>

</mapper>